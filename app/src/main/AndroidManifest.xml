<?xml version="1.0" encoding="utf-8"?><!--
  ~   Copyright 2020–2026 PhotoZ
  ~
  ~   Licensed under the Apache License, Version 2.0 (the "License");
  ~   you may not use this file except in compliance with the License.
  ~   You may obtain a copy of the License at
  ~
  ~        http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~   Unless required by applicable law or agreed to in writing, software
  ~   distributed under the License is distributed on an "AS IS" BASIS,
  ~   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~   See the License for the specific language governing permissions and
  ~   limitations under the License.
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SHORT_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <queries>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto" android:host="*" />
        </intent>
    </queries>

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_launcher_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:enableOnBackInvokedCallback="true">

        <!-- ─── WorkManager on-demand initialization ─────────────────────────── -->
        <!-- BaseApplication implements Configuration.Provider and supplies a custom
             Configuration that wires in HiltWorkerFactory (needed for @HiltWorker
             classes like PhotoSyncWorker that use @AssistedInject — no no-arg ctor).

             For Configuration.Provider to be consulted, the DEFAULT WorkManagerInitializer
             (registered via androidx.startup) MUST be removed. Without this removal,
             androidx.startup initializes WorkManager at app startup with the DEFAULT
             Configuration (default WorkerFactory = reflection + no-arg ctor only),
             BaseApplication's Configuration.Provider is NEVER consulted, and
             @HiltWorker classes fail to instantiate with ENQUEUED→FAILED, runAttemptCount=0,
             no doWork() log lines (the failure happens BEFORE doWork is reached).

             This was the root cause of the "upload worker enqueued but never runs" bug.
             See: https://developer.android.com/guide/background/persistent/configuration/custom-configuration -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- MAIN LAUNCHER: Starts the app -->
        <activity-alias
            android:name=".MainLauncher"
            android:exported="true"
            android:targetActivity=".main.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- STEALTH LAUNCHER: Replaces main launcher and starts the phone app -->
        <activity-alias
            android:name=".StealthLauncher"
            android:enabled="false"
            android:exported="true"
            android:icon="@drawable/ic_phone"
            android:label="@string/hide_app_app_name_stealth"
            android:roundIcon="@drawable/ic_phone"
            android:targetActivity=".forwarddialer.ForwardDialerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- SEND LAUNCHER: Receives photos and videos as send to -->
        <activity-alias
            android:name=".SendLauncher"
            android:exported="true"
            android:targetActivity=".main.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".main.ui.MainActivity"
            android:configChanges="orientation|screenSize" />

        <activity
            android:name=".forwarddialer.ForwardDialerActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".recoverymenu.RecoveryMenuActivity"
            android:theme="@style/AppTheme" />

        <receiver
            android:name=".DialLauncher"
            android:exported="true">
            <intent-filter>
                <action android:name="android.provider.Telephony.SECRET_CODE" />
                <data android:scheme="android_secret_code" />
            </intent-filter>
        </receiver>

        <service android:name=".encryption.migration.MigrationService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!--
            WorkManager foreground service for cloud sync (rclone) — @since PR1 sync feature.
            Declares BOTH "dataSync|shortService" types so the service accepts either:
            - shortService for WorkManager's internal expedited work (3-min timeout)
            - dataSync for manual setForeground() promotion (6-hour timeout)
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync|shortService"
            tools:node="merge"
            android:exported="false" />

        <!--
            FileProvider for the file-upload feature's external viewer.
            DOCUMENT / ARCHIVE / AUDIO files can't be rendered by the in-app
            image viewer — tapping one in the gallery decrypts it to a cache
            file under files/extview/ and hands a content:// URI to Android
            via Intent.ACTION_VIEW. The viewer app resolves the URI through
            this FileProvider.
            @since file-upload feature
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
